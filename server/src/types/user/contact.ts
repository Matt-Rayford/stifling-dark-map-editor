export interface Contact {
	id: string;
	name: string;
	phoneNumber: string;
	email: string;
}

export interface ContactInput {
	name: string;
	phoneNumber: string;
	email: string;
}

export interface DBContact {
	id: string;
	name: string;
	phone_number: string;
	email: string;
}
